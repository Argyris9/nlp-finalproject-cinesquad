# Enforce the "engines" field in package.json -- without this, npm only
# prints a warning on an unsupported Node version and installs anyway,
# which then fails later with a much less readable error from
# @tanstack/start-server-core.
engine-strict=true
